"""通用格式化工具"""


def format_int64(i: int) -> str:
    """将大整数格式化为 B（十亿）或 T（万亿）单位的字符串"""
    if abs(i) < 1_000_000_000_000:
        return f"{i / 1_000_000_000:.2f}B"
    return f"{i / 1_000_000_000_000:.2f}T"
